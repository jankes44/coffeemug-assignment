{
  "name": "coffeemug-assignment",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling for conventional commit enforcement",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "prepare": "husky"
  }
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
